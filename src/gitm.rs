use std::io;
use std::io::prelude::*;
use std::fs::File;

struct addEntry {
	pattern: String,
	path: std::path::PathBuf
}

fn main() {
	let args: Vec<String> = env::args().collect();

}

fn add_entry() {

}

fn open_file() {
	
}

fn include_template() {
	
}